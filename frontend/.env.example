# API base URL (backend)
REACT_APP_API_URL=http://localhost:3001

# Hero image URL (full URL). If not set, uses REACT_APP_ASSETS_BASE_URL + /hero.jpg, else falls back to /hero.jpg
REACT_APP_HERO_IMAGE=

# Base URL (CDN/bucket) for static assets used on Home (destinos e hero). Example: https://cdn.example.com/fastravel
REACT_APP_ASSETS_BASE_URL=
